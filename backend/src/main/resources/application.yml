server:
  port: 8080

spring:
  application:
    name: agent-eval-backend

  # Datasource Configuration
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://114.66.28.183:49543/agent_eval?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: agent_eval
    password: agent_eval_%123

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  # Redis Configuration
  redis:
    host: 114.66.28.183
    port: 28369
    password: agent_eval_%123
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

  # Jackson Configuration
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null

# Logging Configuration
logging:
  level:
    com.example.agenteval: debug
    org.hibernate.SQL: debug

minio:
  endpoint: http://agentevalminio.3q.hair
  access-key: agent_eval
  secret-key: agent_eval_%123
  bucket: eval